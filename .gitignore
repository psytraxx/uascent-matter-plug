build/
build_*/
.cache
compile_commands.json

# nRF Connect / VS Code generated files. settings.json is tracked on purpose:
# it points the extension at this directory as the application root.
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.vscode/nrf-connect.json

# Zephyr/west local state
.west/
twister-out*/

# Seeed XIAO reference pinout images (docs/front.png, docs/back.png).
# ~7.4 MB of upstream artwork; the facts we need are transcribed into
# docs/pinout.md and docs/smart-plug-plan.md. Re-downloadable from the
# Seeed wiki if needed.
docs/*.png
