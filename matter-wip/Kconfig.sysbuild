# No MCUboot on this board (Adafruit UF2 bootloader owns the flash map), so
# neither DFU images nor a factory data partition are generated.
choice BOOTLOADER
	default BOOTLOADER_NONE
endchoice

config MATTER_FACTORY_DATA_GENERATE
	default n

source "share/sysbuild/Kconfig"
